export enum GatewayEvents {
    Ready = 'ready',
    Resumed = 'resumed',
    Closed = 'closed',
    Error = 'error',
    Hello = 'hello',
    Heartbeat = 'heartbeat',
    Dispatch = 'dispatch',
    Debug = 'debug',
}

export enum DispatchEvents {
	Ready = 'READY',
	Resumed = 'RESUMED',
}