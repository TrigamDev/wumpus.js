export enum GatewayEvents {
    Closed = 'closed',
    Debug = 'debug',
    Dispatch = 'dispatch',
    Error = 'error',
    Heartbeat = 'heartbeat',
    Hello = 'hello',
    Ready = 'ready',
    Resumed = 'resumed'
}